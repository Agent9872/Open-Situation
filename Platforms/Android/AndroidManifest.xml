<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

	<!-- ================================================ -->
	<!-- SDK VERSIONS                                    -->
	<!-- ================================================ -->
	<uses-sdk android:minSdkVersion="23"
              android:targetSdkVersion="35" />

	<!-- ================================================ -->
	<!-- APPLICATION CONFIGURATION                       -->
	<!-- ================================================ -->
	<application android:allowBackup="true"
                 android:icon="@mipmap/appicon"
                 android:roundIcon="@mipmap/appicon_round"
                 android:supportsRtl="true"
                 android:requestLegacyExternalStorage="true"
                 android:usesCleartextTraffic="true">

		<!-- Notification Channel Metadata -->
		<meta-data android:name="com.google.firebase.messaging.default_notification_channel_id"
				   android:value="lock_chat_channel" />

		<!-- Firebase Receivers -->
		<receiver android:name="com.google.firebase.iid.FirebaseInstanceIdReceiver"
				  android:exported="true"
				  android:permission="com.google.android.c2dm.permission.SEND">
			<intent-filter>
				<action android:name="com.google.android.c2dm.intent.RECEIVE" />
			</intent-filter>
		</receiver>

		<!-- Plugin.LocalNotification: handles scheduled/alarm notifications -->
		<receiver android:name="Plugin.LocalNotification.Platforms.Android.ScheduledAlarmHandler"
				  android:exported="false">
			<intent-filter>
				<action android:name="LocalNotificationIntent" />
			</intent-filter>
		</receiver>

		<!-- Plugin.LocalNotification: restore alarms after device reboot -->
		<receiver android:name="Plugin.LocalNotification.Platforms.Android.NotificationRestoreReceiver"
				  android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.BOOT_COMPLETED" />
			</intent-filter>
		</receiver>

	</application>

	<!-- ================================================ -->
	<!-- NETWORK & INTERNET PERMISSIONS                  -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />

	<!-- ================================================ -->
	<!-- AUDIO PERMISSIONS (Voice Messages)              -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.RECORD_AUDIO" />
	<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

	<!-- ================================================ -->
	<!-- STORAGE PERMISSIONS                             -->
	<!-- ================================================ -->
	<!-- Legacy storage permissions (Android 12 and below) -->
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" />

	<!-- Android 13+ (API 33+) granular media permissions -->
	<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
	<uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
	<uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

	<uses-permission android:name="android.permission.USE_BIOMETRIC" />
	<uses-permission android:name="android.permission.USE_FINGERPRINT" />

	<!-- ================================================ -->
	<!-- CONTACT & PHONE PERMISSIONS                     -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.CALL_PHONE" />
	<uses-permission android:name="android.permission.READ_CONTACTS" />
	<uses-permission android:name="android.permission.READ_PHONE_STATE" />

	<!-- ================================================ -->
	<!-- SMS PERMISSIONS (SOS Alerts)                    -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.SEND_SMS" />
	<uses-permission android:name="android.permission.RECEIVE_SMS" />
	<uses-permission android:name="android.permission.READ_SMS" />

	<!-- ================================================ -->
	<!-- LOCATION PERMISSIONS (SOS & Live Features)      -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

	<!-- For background location access (optional, for continuous monitoring) -->
	<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

	<!-- ================================================ -->
	<!-- NOTIFICATION PERMISSIONS (Android 13+)          -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

	<!-- ================================================ -->
	<!-- BATTERY & BACKGROUND PERMISSIONS                -->
	<!-- ================================================ -->
	<!-- Wake lock for notifications when screen is off -->
	<uses-permission android:name="android.permission.WAKE_LOCK" />

	<!-- Prevent battery optimization for reliable notifications -->
	<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

	<!-- For foreground service notifications -->
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

	<!-- ================================================ -->
	<!-- CAMERA PERMISSIONS (For future video calls)     -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.CAMERA" />

	<!-- Optional: For flashlight in camera -->
	<uses-permission android:name="android.permission.FLASHLIGHT" />

	<!-- ================================================ -->
	<!-- VIBRATION PERMISSION                            -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.VIBRATE" />

	<!-- ================================================ -->
	<!-- BOOT COMPLETED PERMISSION (Plugin.LocalNotification) -->
	<!-- ================================================ -->
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

	<!-- ================================================ -->
	<!-- QUERIES (For opening external apps)             -->
	<!-- ================================================ -->
	<queries>
		<!-- File picker -->
		<intent>
			<action android:name="android.intent.action.VIEW" />
			<data android:mimeType="*/*" />
		</intent>

		<!-- Phone dialer -->
		<intent>
			<action android:name="android.intent.action.DIAL" />
			<data android:scheme="tel" />
		</intent>

		<!-- SMS -->
		<intent>
			<action android:name="android.intent.action.SENDTO" />
			<data android:scheme="smsto" />
		</intent>

		<!-- WhatsApp -->
		<intent>
			<action android:name="android.intent.action.VIEW" />
			<data android:scheme="https" android:host="api.whatsapp.com" />
		</intent>

		<!-- Email -->
		<intent>
			<action android:name="android.intent.action.SENDTO" />
			<data android:scheme="mailto" />
		</intent>
	</queries>

</manifest>